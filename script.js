// Simulasi data kinerja
const productionData = 1200; // Contoh data produksi harian
const efficiencyData = 85; // Contoh data efisiensi operasional

// Update elemen di dashboard
document.getElementById('production').innerText = productionData;
document.getElementById('efficiency').innerText = efficiencyData + '%';